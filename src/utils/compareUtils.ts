export const compareTexts = (text1: string, text2: string) => {
    // Implementa tu lógica de comparación aquí
    // Por ejemplo, una comparación simple podría ser:
    return text1 === text2 ? "Los textos son iguales" : "Los textos son diferentes";
};
